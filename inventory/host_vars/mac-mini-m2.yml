---
# =============================================================================
# Mac Mini M2 — Bot Machine (headless)
# Groups: headless + agent + thailand
# =============================================================================
# headless group: no GUI apps
# agent group:    automation tooling (future)
# Result: minimal install — core formulae + Tailscale + 1Password only
# =============================================================================

homebrew_formulae_extra: []
homebrew_casks_extra: []
git_repos_extra: []
