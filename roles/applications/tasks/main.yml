---
- name: Install Homebrew cask applications
  community.general.homebrew_cask:
    name: "{{ item }}"
    state: present
  loop: "{{ homebrew_casks + homebrew_casks_extra }}"
  when: (homebrew_casks + homebrew_casks_extra) | length > 0
